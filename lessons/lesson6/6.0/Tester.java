public class Tester {
    public static void main(String[] args) {
        int[] m = { 1, 2, 3, 4, 5, 6 };
        int[] m1 = { -1, 9, 4, 56 };
        double[] m2 = { 1, 2, 3, 4, 5, 6 };

        // Tar0.ArrChangeOrder(new int[] {1,2,3,4,5,6});
        // Tar0.ToBinary(13);
        // Tar0.Swap();
        // System.out.println( Tar0.maxFirstLast(m) );
        // Tar0.ArrToStringPrint(Tar0.getFirstLast(m) );
        // System.out.println(Tar0.med(m2) );
        //Tar0.ArrToStringPrint(Tar0.mergeStartEnd(m, m1));
        //Tar0.ArrToStringPrint(Tar0.connect2Arr(m, m1));
        //Tar0.Create100Arr();
        //Tar3.oddCoupele(m);
    }
}
